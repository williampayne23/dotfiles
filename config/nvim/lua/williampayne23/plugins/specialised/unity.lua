return {}
-- This doesn't work as it hooks into nvim tree events and I use snacks tree :(
-- return {
--     "apyra/nvim-unity-sync",
--     lazy = false,
--     config = function()
--         require("unity.plugin").setup()
--     end,
-- }
